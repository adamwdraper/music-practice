define([
    'Underscore',
    'Backbone'
], function(_, Backbone) {
    var LessonModel = Backbone.Model.extend({

        defaults: {},

        initialize: function(){
        }

    });

    return LessonModel;

});